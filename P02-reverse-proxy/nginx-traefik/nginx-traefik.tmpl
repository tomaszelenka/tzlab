<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <title>p02 Traefik</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      margin: 2rem;
    }

    .card {
      border: 1px solid #ddd;
      border-radius: 12px;
      padding: 1.5rem;
      max-width: 700px;
    }

    h1 {
      font-size: 1.5rem;
      margin-top: 0;
      margin-bottom: 1rem;
    }

    p {
      font-size: 1.1rem;
      line-height: 1.5;
    }

    code {
      background: #f6f8fa;
      padding: .2rem .4rem;
      border-radius: 6px;
    }

    .under {
      text-decoration: underline;
      text-underline-offset: 3px;
      text-decoration-thickness: 2px;
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>✅ Nginx served through Traefik</h1>
    <p>Hostname node: <code><span class="under"><strong>$HOSTNAME</strong></span></code></p>
    <p>Service: <code>$SERVICE_NAME</code></p>
    <p>Task slot: <code>${TASK_SLOT}</code></p>
  </div>
</body>

</html>
